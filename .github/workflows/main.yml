name: ci

on:
  push:
  pull_request:
    branches: [main, develop]

jobs:
  ci:
    runs-on: ubuntu-latest
    name: ci
    env:
      DAGGER_CLOUD_TOKEN: ${{ secrets.DAGGER_CLOUD_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup mise
        uses: jdx/mise-action@v2
        with:
          install: true

      - name: Setup task
        uses: arduino/setup-task@v2

      - name: Cache Go modules and build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Setup Dagger
        uses: dagger/dagger-for-github@v7

      - name: Wait for Docker daemon
        shell: bash
        run: |
          set -euo pipefail
          for i in {1..20}; do
            if docker info >/dev/null 2>&1; then
              exit 0
            fi
            sleep 3
          done
          docker info

      - name: Clean stale Dagger engines
        shell: bash
        run: |
          set -euo pipefail
          docker ps -a --format '{{.Names}}' | grep '^dagger-engine' | xargs -r docker rm -f || true

      - name: Dagger version
        shell: bash
        run: |
          set -euo pipefail
          if ! dagger version; then
            docker ps -a --format '{{.Names}}' | grep '^dagger-engine' | xargs -r docker rm -f || true
            dagger version
          fi

      - name: Run CI task via Dagger
        shell: bash
        run: |
          set -euo pipefail
          if ! dagger run -- mise exec -- task ci; then
            echo "First Dagger run failed; cleaning stale engine container(s) and retrying once..."
            docker ps -a --format '{{.Names}}' | grep '^dagger-engine' | xargs -r docker rm -f || true
            dagger run -- mise exec -- task ci
          fi

  pact-tests:
    runs-on: ubuntu-latest
    name: pact-tests
    needs: [ci]
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/develop'
    env:
      DAGGER_CLOUD_TOKEN: ${{ secrets.DAGGER_CLOUD_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup mise
        uses: jdx/mise-action@v2
        with:
          install: true

      - name: Setup task
        uses: arduino/setup-task@v2

      - name: Cache Go modules and build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Cache Pact FFI runtime
        uses: actions/cache@v4
        with:
          path: .pact/lib
          key: ${{ runner.os }}-pactffi-${{ hashFiles('Taskfile.yml') }}
          restore-keys: |
            ${{ runner.os }}-pactffi-

      - name: Setup Dagger
        uses: dagger/dagger-for-github@v7

      - name: Wait for Docker daemon
        shell: bash
        run: |
          set -euo pipefail
          for i in {1..20}; do
            if docker info >/dev/null 2>&1; then
              exit 0
            fi
            sleep 3
          done
          docker info

      - name: Clean stale Dagger engines
        shell: bash
        run: |
          set -euo pipefail
          docker ps -a --format '{{.Names}}' | grep '^dagger-engine' | xargs -r docker rm -f || true

      - name: Run pact tests task via Dagger
        shell: bash
        run: |
          set -euo pipefail
          if ! dagger run -- mise exec -- task pact:install test:pact; then
            echo "First Dagger run failed; cleaning stale engine container(s) and retrying once..."
            docker ps -a --format '{{.Names}}' | grep '^dagger-engine' | xargs -r docker rm -f || true
            dagger run -- mise exec -- task pact:install test:pact
          fi

  release:
    runs-on: ubuntu-latest
    name: release
    needs: [ci]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    env:
      DAGGER_CLOUD_TOKEN: ${{ secrets.DAGGER_CLOUD_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Dagger
        uses: dagger/dagger-for-github@v7

      - name: Wait for Docker daemon
        shell: bash
        run: |
          set -euo pipefail
          for i in {1..20}; do
            if docker info >/dev/null 2>&1; then
              exit 0
            fi
            sleep 3
          done
          docker info

      - name: Clean stale Dagger engines
        shell: bash
        run: |
          set -euo pipefail
          docker ps -a --format '{{.Names}}' | grep '^dagger-engine' | xargs -r docker rm -f || true

      - name: Dagger version
        shell: bash
        run: |
          set -euo pipefail
          if ! dagger version; then
            docker ps -a --format '{{.Names}}' | grep '^dagger-engine' | xargs -r docker rm -f || true
            dagger version
          fi

      - name: Semver release
        id: release
        uses: cocogitto/cocogitto-action@v3.5
        with:
          release: true
          git-user: 'version'
          git-user-email: 'info@sys-int.de'